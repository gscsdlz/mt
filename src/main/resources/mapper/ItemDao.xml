<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mt.dao.ItemDao">
    <select id="getAllItem" resultType="Item">
        SELECT * FROM m_item WHERE store_id = #{store_id};
    </select>
    <insert id="addItem">
        INSERT INTO m_item (item_name, store_id, old_price, price, item_img) VALUES (#{item_name}, #{store_id}, #{old_price}, #{price}, #{item_img});
    </insert>
    <update id="updateItem">
        UPDATE m_item SET item_name = #{item_name}, item_img = #{item_img}, old_price = #{old_price}, price = #{price} WHERE id = #{id};
    </update>
    <delete id="delItem">
        DELETE FROM m_item WHERE id = #{id};
    </delete>
</mapper>